feat: accept client-parsed workbook via apply endpoint

Add POST /schedule-import/apply that takes derived schedule metadata
(part keys, titles, durations) parsed in the admin's browser and reuses
the same idempotent per-week upsert as the upload flow. The publication
file no longer needs to reach the server. The upload route is kept as a
fallback for one release.

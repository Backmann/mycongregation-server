feat: expose partKey on task items

Adds partKey to the My Tasks payload (MyAssignmentItem) for meeting
items, so the client can tailor how a part is displayed (e.g. show only
the topic for the opening Treasures talk, matching the schedule screen).

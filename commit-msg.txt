feat: expose partOrder on task items

Adds partOrder to the My Tasks payload so the client can sort the parts
of a single day in program order (chairman first, closing prayer last)
instead of arrival order.

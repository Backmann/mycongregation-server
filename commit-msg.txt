feat: support silent publish (notify flag)

publishMeeting always sent a push when a meeting went live. Added an
optional notify flag (default true, so existing callers are unchanged):
the push to the congregation now fires only when notify !== false,
enabling a silent publish. Threaded through PublishAssignmentsDto and
the controller.
